[project]
name = "mazegen"
version = "0.1.0"
description = "Maze generator"
authors = [
    {name = "mamiandr",email = "mamiandr@.42antananarivo.mg"}
]
packages = [{ include = "mazegen", from = "src" }]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2.7"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
